import React from "react";
import { NavLink } from "react-router-dom"

function NavBar() {
    return (
        <nav>
            <NavLink exact to="/">Home</NavLink>
            <NavLink exact to="/ideas/:id">My Ideas</NavLink>
            <NavLink exact to="/idea/new">Add Idea</NavLink>
        </nav>
    );
}

export default NavBar;
